# Retrieval Baselines Repository

This repository contains the code and experiment configurations used to evaluate multiple retrieval baselines. Each baseline is organized in its own directory and includes the corresponding job scripts required to run experiments.

## Running Experiments

Navigate to the desired baseline directory and execute the relevant job script
